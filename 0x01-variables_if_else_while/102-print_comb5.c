#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * Description: Write a script that print all combination of two digits
 * Return: 0
 *
 */
int main(void)
{
int p, q;
{
for (p = 0; p = < 98; p++)
{
for (q = 0; q = < 98; q++)
{
if (p != q)
putchar((p / 10) + '0');
putchar((p % 10) + '0');
putchar((q / 10) + '0');
putchar(' ');
putchar((q % 10) + '0');
while (p = 98 && q = 99)
	continue;
putchar(',');
putchar(' ');
}
}
}
}
putchar('\n');
return (0);
}

